{
  "//": "App hosting + serverless functions live on Vercel (see vercel.json). Firebase is used only for Auth + Firestore. This file just defines the Firestore rules/indexes (deploy with `npm run deploy:rules`) and the local emulators.",
  "firestore": {
    "rules": "firestore.rules",
    "indexes": "firestore.indexes.json"
  },
  "emulators": {
    "auth": { "port": 9099 },
    "firestore": { "port": 8080 },
    "ui": { "enabled": true, "port": 4000 },
    "singleProjectMode": true
  }
}
